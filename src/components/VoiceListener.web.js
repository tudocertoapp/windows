/**
 * VoiceListener - fallback web: reconhecimento de voz indisponível.
 * Componente invisível que não faz nada.
 */
import React, { forwardRef } from 'react';

export const VoiceListener = forwardRef(function VoiceListener() {
  return null;
});
